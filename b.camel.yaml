- route:
    id: b
    from:
      uri: "platform-http:/b"
      steps:
        - log:
            message: "B: ${body}"
        - setBody:
            expression:
              constant:
                expression: Hi from B!
