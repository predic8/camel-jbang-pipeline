- route:
    id: test-1
    from:
      id: from-4452
      uri: timer
      parameters:
        period: "1000"
        timerName: template
      steps:
        - log:
            id: log-3810
            message: ${body}
